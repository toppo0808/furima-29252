require 'rails_helper'

RSpec.describe Pay, type: :model do
#   before do
#     user FactoryBot.create(:user)
#     @pay = FactoryBot.build(:pay, user_id: user.id, item_id: item.id)
#   describe '購入機能' do
#   it "全ての項目があると購入できる。" do
#     expect(@pay).to be_valid
#   end
  
#   it "user_idがないと購入できない" do
#     @pay.user_id = nil
#     @pay.valid?
#     expect(@pay.errors.full_messages).to include("user_id can't be blank.")
#   end

#   it "item_idがないと購入できない" do
#   end

#   end
#  end
end
